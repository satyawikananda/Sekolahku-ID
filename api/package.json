{
  "name": "api-sekolahku",
  "version": "1.0.0",
  "main": "dist/server.js",
  "license": "MIT",
  "scripts": {
    "start:build": "tsc",
    "start:prebuild": "tslint -c tslint.json -p tsconfig.json --fix",
    "start:dev": "nodemon .",
    "prestart": "yarn start:build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@types/express": "^4.17.6",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "typescript": "^3.9.3"
  },
  "devDependencies": {
    "@types/node": "^14.0.9",
    "nodemon": "^2.0.4",
    "tslint": "^6.1.2"
  }
}
